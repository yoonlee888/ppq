kind: pipeline
type: docker
name: citest


steps:
- name: test
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
  - docker ps -a
  - sleep 1000
  - docker run --net host --rm --privileged --shm-size 100g --ulimit memlock=-1 --env MTHREADS_VISIBLE_DEVICES=all --pid=host -v /drone/src:/data/qiuyunlei --name test_ci1 dockerhub.kubekey.local/mt-ai/lm-qy2:kuae_1.1_0420_train /bin/bash 

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock
