kind: pipeline
type: docker
name: citest


steps:
- name: test
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  - name: codebase
    path: /drone/src
  commands:
  - docker ps -a
  - sleep 1000
  - docker run --net host --rm --privileged --shm-size 100g --ulimit memlock=-1 --env MTHREADS_VISIBLE_DEVICES=all --pid=host -v /drone/src:/data/qiuyunlei --name test_ci12 dockerhub.kubekey.local/mt-ai/lm-qy2:add_sh sh -c "bash /data/ci/af_build.sh"


volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock
- name: codebase
  host:
    path: /home/codebase
